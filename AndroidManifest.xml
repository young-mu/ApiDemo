<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.young.jniinterface"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:debuggable="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DowncallActivity"
            android:label="@string/downcall_button" >
        </activity>

        <activity
            android:name=".DowncallOnloadActivity"
            android:label="@string/downcall_onload_button" >
        </activity>
        <activity
            android:name=".UpcallActivity"
            android:label="@string/upcall_button" >
        </activity>
        <activity
            android:name=".InvokeActivity"
            android:label="@string/invoke_button" >
        </activity>
        <activity
            android:name=".SignalActivity"
            android:label="@string/signal_button" >
        </activity>
        <activity
            android:name=".SyscallActivity"
            android:label="@string/syscall_button" >
        </activity>
    </application>

</manifest>
